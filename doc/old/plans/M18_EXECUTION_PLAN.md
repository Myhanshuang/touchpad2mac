# M18 Execution Plan

1. Core gesture trigger/target schema + routing state.
2. Integrate optional routing in `ArbiterConfig` and the M14/M15 ownership path.
3. Add `UserSettings` + `M18Profile` with M17-compatible defaults.
4. Add CLI settings editor/preset/offline GUI and `m18-remap-v1 --settings`.
5. Add public tests, docs, full gates, review.

No live device/portal/KDE action transport is invoked during implementation.
